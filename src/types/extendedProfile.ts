import {Profile} from "./profile";

export interface ExtendedProfile extends Profile {
    pastEvents: any[]; // Replace any[] with the appropriate type
    organizedEvents: any[]; // Replace any[] with the appropriate type
}